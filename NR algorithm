def NR (p, n, e):
    a = p
    b = 1
    while (a - b) > e:
        for i in range (n):
            a, b = (a + b) / 2, p / a
            # print (f"a = {a} b = {b}")
    return a
print (NR (49, 106, 0.001))
